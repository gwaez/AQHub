/**
 * P2+ stub — Magic Wand / UI Automation lives here later.
 * Do not put UIA or window-hook code in animation or the state machine.
 */
export const MAGIC_WAND_PHASE = "later";
export function isMagicWandAvailable(): boolean {
  return false;
}
